import { render, screen } from '@testing-library/react';
import Home from '@pages/index';
// 추후 테스트 코드 작성
describe('Homepage', () => {
  it('renders a home', () => {
    expect(1 + 2).toBe(3);
  });
});
